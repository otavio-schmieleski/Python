<TelaGridLayout>
    GridLayout:
        # pode ser passado apenas a linha ou a coluna
        # vai ter 3 colunas
        cols: 3
        # vai ter colunas conforme a demnada da janela, quando acabar as 3 colunas ele cria a outra linha
        rows: None

        # aqui vai setar o tamanho altura do botoes de 50%
        #row_default_height: 50
        # aqui vai forca a janela a utilizar os 50% passados
        #row_force_default: True

        # aqui vai setar o tamanho da largura do botoes de 50%
        #col_default_width: 50
        # aqui vai forca a janela a utilizar os 50% passados
        #col_force_default: True

        Button:
            text:'btn1'
        Button:
            text:'btn2'
        Button:
            text:'btn3'
        Button:
            text:'btn4'